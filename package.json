{
  "name": "personal-ops-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo full-stack app with Express serving Vite build",
  "scripts": {
    "dev": "concurrently -k -n CLIENT,SERVER -c cyan,green \"npm run client\" \"npm run server\"",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "frontend": "npm run client",
    "backend": "npm run server",
    "password:reset": "npm run password:reset --prefix server --",
    "build": "npm install --prefix server && npm install --prefix client --include=dev && npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
